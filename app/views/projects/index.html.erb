<%= link_to "Login", {:controller=> "sessions", :action=> "new"}, :method => :get %> |
<%= link_to "Sign Up", {:controller=> "users", :action=> "new"}, :method => :get %>




<h1>All Projects</h1>



<ul>
  <% @projects.each do |project| %>
    <%= link_to project.name, project_path(project[:id]) %>
  <% end %>
</ul>
